---
id: "06-02"
phase: 6
wave: 2
name: "Dockerfile + Docker Swarm stack"
objective: "Multi-stage Dockerfile, docker-compose.yml stack for yowanet overlay network — production deployment on Portainer stack ID 8 (OPS-03 deployment)"
autonomous: true
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
requirement_ids:
  - OPS-03
---

# Plan 06-02: Dockerfile + Docker Swarm stack

## Objective

Create production-ready container artifacts:
1. Multi-stage `Dockerfile` (build → production) using Node.js 22 Alpine
2. `docker-compose.yml` (Swarm stack) on `yowanet` overlay network connecting to `postgres` and `evolution` by service name
3. `.dockerignore` to exclude dev artifacts

## Context

- **Rede**: `yowanet` overlay network (already exists in Portainer stack)
- **Banco**: PostgreSQL service name `postgres` on port 5432 (shared cluster, stack ID 8)
- **Evolution**: Evolution API service name `evolution` on port 8080
- **Node 22 Alpine**: `node:22-alpine` base image — matches CLAUDE.md constraint
- **SIGTERM**: Already handled in `src/index.ts` (Phase 5) — `stop_grace_period: 30s` in stack
- **Env vars**: All injected via Swarm secrets / env in docker-compose.yml (no .env in production)
- **Port**: `PORT` default 3000, exposed on host as needed
- **Build**: `npm ci --omit=dev` for production deps, `npm run build` (tsc to dist/)

## Tasks

### Task 1 — Add build script to package.json

Add `"build": "tsc"` script and verify tsconfig.json has `outDir: "./dist"`. The Dockerfile uses `npm run build`.

Commit: `chore(06): add build script and tsconfig outDir for production build`

### Task 2 — Create `.dockerignore`

```
node_modules
dist
.planning
.env
*.md
.git
data
```

Commit: `chore(06): add .dockerignore`

### Task 3 — Create `Dockerfile`

```dockerfile
# Stage 1: Build
FROM node:22-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY tsconfig.json ./
COPY src ./src
RUN npm run build

# Stage 2: Production
FROM node:22-alpine AS production
WORKDIR /app
ENV NODE_ENV=production
COPY package*.json ./
RUN npm ci --omit=dev
COPY --from=builder /app/dist ./dist
EXPOSE 3000
CMD ["node", "dist/index.js"]
```

Commit: `feat(06): add multi-stage Dockerfile (Node 22 Alpine)`

### Task 4 — Create `docker-compose.yml`

```yaml
version: "3.8"

services:
  brainny:
    image: brainny:latest
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      HOST: "0.0.0.0"
      PORT: "3000"
      LOG_LEVEL: info
      # Secrets via Swarm environment / Portainer
      DATABASE_URL: "${DATABASE_URL}"
      OPENAI_API_KEY: "${OPENAI_API_KEY}"
      WEBHOOK_SECRET: "${WEBHOOK_SECRET}"
      SEARCH_TOKEN: "${SEARCH_TOKEN}"
      EVOLUTION_URL: "http://evolution:8080"
      EVOLUTION_API_KEY: "${EVOLUTION_API_KEY}"
      EVOLUTION_INSTANCE: "${EVOLUTION_INSTANCE:-brainny}"
      DATA_DIR: "/app/data"
      TIMEZONE: "America/Sao_Paulo"
    volumes:
      - brainny_data:/app/data
    ports:
      - "3000:3000"
    networks:
      - yowanet
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3
      stop_grace_period: 30s
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  brainny_data:
    driver: local

networks:
  yowanet:
    external: true
```

Commit: `feat(06): add docker-compose.yml for yowanet Swarm deployment (OPS-03)`

### Task 5 — Full suite gate

```bash
npx tsc --noEmit && npx vitest run
```

All tests must still pass.

## Must-Haves

- [ ] `Dockerfile` uses Node 22 Alpine multi-stage build
- [ ] `docker-compose.yml` uses `yowanet` as external network
- [ ] Services `postgres` and `evolution` reachable by name within stack
- [ ] `stop_grace_period: 30s` aligned with graceful shutdown timeout
- [ ] `/health` used as healthcheck endpoint
- [ ] `DATA_DIR` mounted as a persistent volume
- [ ] `npx tsc --noEmit` exits 0
- [ ] `npx vitest run` passes all tests
