---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - drizzle.config.ts
  - src/db/schema.ts
  - src/db/client.ts
  - src/lib/logger.ts
  - src/plugins/config.ts
  - src/plugins/db.ts
  - src/routes/health.ts
  - src/routes/health.test.ts
  - src/index.ts
autonomous: true
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
  - OPS-01

must_haves:
  truths:
    - "GET /health returns {ok: true, ts, db: 'ok'} with status 200 when DB is reachable"
    - "GET /health returns {ok: false, ts, db: 'error'} with status 503 when DB query fails"
    - "npm run dev boots Fastify, validates env, connects to DB, binds to configured port"
    - "messages table schema has vector(1536) column named embedding"
    - "chats table schema has name, is_group, participants_json columns"
    - "sync_state table schema has key (PK), value (jsonb), updated_at columns"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Drizzle table definitions for messages, chats, sync_state"
      exports: ["messages", "chats", "syncState", "Message", "NewMessage", "Chat", "NewChat", "SyncState"]
    - path: "src/db/client.ts"
      provides: "pg.Pool factory function"
      exports: ["createPool"]
    - path: "drizzle.config.ts"
      provides: "drizzle-kit configuration"
      contains: "defineConfig"
    - path: "src/plugins/db.ts"
      provides: "Fastify plugin: fastify.db (Drizzle) + fastify.pgPool (pg.Pool)"
      contains: "fp(dbPlugin"
    - path: "src/plugins/config.ts"
      provides: "Fastify plugin: fastify.config decorator"
      contains: "fp(configPlugin"
    - path: "src/routes/health.ts"
      provides: "GET /health route — db probe, 200/503"
      contains: "/health"
    - path: "src/index.ts"
      provides: "Fastify app bootstrap and HTTP listener"
      contains: "loadConfig"
    - path: "src/routes/health.test.ts"
      provides: "Integration tests for OPS-01"
  key_links:
    - from: "src/plugins/db.ts"
      to: "src/plugins/config.ts"
      via: "dependencies: ['config'] in fp() call"
      pattern: "dependencies.*config"
    - from: "src/routes/health.ts"
      to: "fastify.pgPool"
      via: "fastify.pgPool.query('SELECT 1')"
      pattern: "pgPool\\.query"
    - from: "src/index.ts"
      to: "src/config.ts"
      via: "loadConfig() called before Fastify instantiation"
      pattern: "loadConfig\\(\\)"
    - from: "src/db/schema.ts"
      to: "drizzle-orm/pg-core"
      via: "vector column type import"
      pattern: "vector.*1536"
---

<objective>
Build the DB schema layer and the full Fastify application skeleton: Drizzle schema with messages/chats/sync_state tables and HNSW index declaration, drizzle.config.ts, Fastify app with config + DB plugins, GET /health route, and integration tests. This plan produces a bootable server with a live health endpoint.

Purpose: After this plan, `npm run dev` runs end-to-end — server starts, validates config, connects to DB, and health endpoint responds. This is the Walking Skeleton moment: a real user action (GET /health) completes a real DB round-trip.
Output: Bootable Fastify app + full DB schema ready for migration in Plan 03.
</objective>

<execution_context>
@C:/Users/Usuario12/OneDrive - HASAS/Obsidian/knowledge/brainny/.planning/phases/01-foundation/01-RESEARCH.md
</execution_context>

<context>
@C:/Users/Usuario12/OneDrive - HASAS/Obsidian/knowledge/brainny/.planning/ROADMAP.md
@C:/Users/Usuario12/OneDrive - HASAS/Obsidian/knowledge/brainny/.planning/REQUIREMENTS.md
@C:/Users/Usuario12/OneDrive - HASAS/Obsidian/knowledge/brainny/.planning/phases/01-foundation/01-SKELETON.md

Key patterns from RESEARCH.md to follow exactly:
- Pattern 1: Fastify 5 + ZodTypeProvider Setup (src/index.ts)
- Pattern 2: Config Plugin (src/plugins/config.ts) — fp() required
- Pattern 3: DB Plugin (src/plugins/db.ts) — fp() required, dependencies: ['config']
- Pattern 5: Drizzle Schema (src/db/schema.ts) — full schema including HNSW index declaration
- Pattern 6: Migration workflow reference (drizzle.config.ts)
- Pattern 7: GET /health Endpoint (src/routes/health.ts)
- Critical: setValidatorCompiler AND setSerializerCompiler BOTH required (PII risk if missing serializer)
- Critical: All local imports must use .js extension (ESM)
- Critical: No console.* anywhere — use fastify.log.*

Prior plan output (01-01): src/config.ts exports loadConfig() and Env type. Import from './config.js'.
</context>

<interfaces>
From src/config.ts (created in Plan 01-01):
  export const envSchema: z.ZodObject<{...}>
  export type Env = z.infer<typeof envSchema>  // { NODE_ENV, PORT, HOST, DATABASE_URL, OPENAI_API_KEY, WEBHOOK_SECRET, SEARCH_TOKEN, DATA_DIR, INGEST_CONCURRENCY, MATERIALIZER_CRON, TZ, LOG_LEVEL }
  export function loadConfig(): Env  // safeParse(process.env) → exit(1) on failure
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DB layer — src/db/schema.ts, src/db/client.ts, drizzle.config.ts</name>
  <files>src/db/schema.ts, src/db/client.ts, drizzle.config.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md Pattern 5 (full Drizzle schema — replicate exactly), Pattern 6 (drizzle.config.ts)
    - .planning/phases/01-foundation/01-RESEARCH.md §Common Pitfalls Pitfall 1 (HNSW WITH parameters), Pitfall 2 (CREATE EXTENSION ordering)
    - .planning/REQUIREMENTS.md STORE-01, STORE-02, STORE-03 descriptions
  </read_first>
  <action>
    Create src/db/schema.ts implementing RESEARCH.md Pattern 5 exactly. Required imports from 'drizzle-orm/pg-core': pgTable, text, timestamp, boolean, jsonb, index, vector. Required imports from 'drizzle-orm': sql.

    messages table columns: id (text PK), chatId (text 'chat_id' notNull), sender (text notNull), senderName (text 'sender_name' nullable), timestamp (timestamp with timezone notNull), type (text notNull), text (text nullable), mediaUrl (text 'media_url' nullable), rawJson (jsonb 'raw_json' notNull — STORE-02), embedding (vector 'embedding' dimensions 1536 nullable), createdAt (timestamp with timezone notNull default sql`now()`).

    messages table indexes (3rd arg table callback returning array): messages_embedding_hnsw using hnsw on embedding.op('vector_cosine_ops') with { m: 16, ef_construction: 64 } (STORE-01), messages_chat_ts_idx on chatId + timestamp, messages_ts_idx on timestamp.

    chats table columns: id (text PK), name (text nullable), isGroup (boolean 'is_group' notNull default false — STORE-03), participantsJson (jsonb 'participants_json' nullable — STORE-03), lastSeenAt (timestamp 'last_seen_at' with timezone nullable).

    syncState table columns: key (text PK), value (jsonb notNull), updatedAt (timestamp 'updated_at' with timezone notNull default sql`now()`).

    Export inferred types: Message, NewMessage (typeof messages.$inferSelect / $inferInsert), Chat, NewChat, SyncState.

    Create src/db/client.ts: import pg from 'pg'; export function createPool(connectionString: string): pg.Pool — returns new pg.Pool({ connectionString, max: 10, idleTimeoutMillis: 30_000 }). Do not call pool.connect() here — that is done in dbPlugin.

    Create drizzle.config.ts implementing RESEARCH.md Pattern 6: import defineConfig from 'drizzle-kit'; export default defineConfig({ dialect: 'postgresql', schema: './src/db/schema.ts', out: './src/db/migrations', dbCredentials: { url: process.env['DATABASE_URL']! }, migrations: { table: '__drizzle_migrations', schema: 'public' } }).
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario12/OneDrive - HASAS/Obsidian/knowledge/brainny" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/db/schema.ts exports: messages, chats, syncState tables and Message, NewMessage, Chat, NewChat, SyncState types
    - src/db/schema.ts contains vector('embedding', { dimensions: 1536 }) — exact string
    - src/db/schema.ts contains .using('hnsw', t.embedding.op('vector_cosine_ops')).with({ m: 16, ef_construction: 64 }) — exact parameters
    - src/db/schema.ts contains rawJson (jsonb column) in messages table
    - src/db/schema.ts contains participantsJson and isGroup columns in chats table
    - src/db/client.ts exports createPool function
    - drizzle.config.ts contains dialect: 'postgresql' and schema: './src/db/schema.ts'
    - tsc --noEmit exits 0 (all three new files type-check)
  </acceptance_criteria>
  <done>DB schema types compile cleanly; messages has vector(1536) + HNSW index declaration; chats has isGroup + participantsJson; sync_state table defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fastify app — plugins, health route, health tests, src/index.ts</name>
  <files>src/lib/logger.ts, src/plugins/config.ts, src/plugins/db.ts, src/routes/health.ts, src/routes/health.test.ts, src/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md Pattern 1 (Fastify 5 + ZodTypeProvider), Pattern 2 (Config Plugin), Pattern 3 (DB Plugin), Pattern 7 (GET /health)
    - .planning/phases/01-foundation/01-RESEARCH.md §Anti-Patterns (fp() omitted, plugin registration order, missing serializerCompiler)
    - .planning/phases/01-foundation/01-VALIDATION.md (test map for OPS-01)
    - src/config.ts (read to confirm Env type shape before writing plugins that consume it)
    - src/db/schema.ts (read to confirm exports before importing into db.ts)
  </read_first>
  <behavior>
    - Test (OPS-01 happy path): Build app with working pgPool mock → GET /health → 200 { ok: true, ts: ISO string, db: 'ok' }
    - Test (OPS-01 error path): Build app with pgPool.query that throws → GET /health → 503 { ok: false, ts: ISO string, db: 'error' }
    - Test (OPS-01 ts field): Response ts field is a valid ISO 8601 date string
  </behavior>
  <action>
    Create src/lib/logger.ts: import pino from 'pino'; export function createLogger(level: string): pino.Logger — returns pino({ level, redact: ['*.connectionString', '*.DATABASE_URL', '*.password'] }). Export type is pino.Logger.

    Create src/plugins/config.ts implementing RESEARCH.md Pattern 2 exactly: import fp from 'fastify-plugin'; import type { FastifyPluginAsync } from 'fastify'; import type { Env } from '../config.js'; declare module 'fastify' { interface FastifyInstance { config: Env } }. The plugin wraps async (fastify, opts) => { fastify.decorate('config', opts.config) }; export default fp(configPlugin, { name: 'config' }). Plugin type: FastifyPluginAsync<{ config: Env }>.

    Create src/plugins/db.ts implementing RESEARCH.md Pattern 3 exactly: import fp from 'fastify-plugin'; import type { FastifyPluginAsync } from 'fastify'; import { drizzle, type NodePgDatabase } from 'drizzle-orm/node-postgres'; import pg from 'pg'; import * as schema from '../db/schema.js'; declare module 'fastify' { interface FastifyInstance { db: NodePgDatabase<typeof schema>; pgPool: pg.Pool } }. Plugin body: create pool via createPool(fastify.config.DATABASE_URL) from '../db/client.js', await pool.query('SELECT 1') for fast-fail, decorate db and pgPool, add onClose hook to pool.end(). Export: fp(dbPlugin, { name: 'db', dependencies: ['config'] }).

    Create src/routes/health.ts implementing RESEARCH.md Pattern 7 exactly: import type { FastifyPluginAsyncZod } from 'fastify-type-provider-zod'; import { z } from 'zod'; define healthResponseSchema as z.object({ ok: z.boolean(), ts: z.string(), db: z.enum(['ok', 'error']) }); route handler: try pgPool.query('SELECT 1') → 200 { ok: true, ts: new Date().toISOString(), db: 'ok' }; catch → log warn + 503 { ok: false, ts: new Date().toISOString(), db: 'error' }. NOTE from RESEARCH.md: use 503 for DB failure (not 200 with db:'error') — the ROADMAP says "non-200 when DB is not reachable".

    Create src/index.ts implementing RESEARCH.md Pattern 1: import Fastify from 'fastify'; import { serializerCompiler, validatorCompiler, type ZodTypeProvider } from 'fastify-type-provider-zod'; import { loadConfig } from './config.js'; import configPlugin from './plugins/config.js'; import dbPlugin from './plugins/db.js'; import healthRoutes from './routes/health.js'; import { createLogger } from './lib/logger.js'. In main(): call loadConfig(), create pino logger via createLogger(config.LOG_LEVEL), create app with Fastify({ logger }), set BOTH validatorCompiler AND serializerCompiler (critical — prevents PII leak), register configPlugin with { config }, register dbPlugin, register healthRoutes on api = app.withTypeProvider<ZodTypeProvider>(). Call app.listen({ port: config.PORT, host: config.HOST }). Load dotenv only if NODE_ENV !== 'production': import('dotenv').then(d => d.config()).

    Create src/routes/health.test.ts covering the 3 behaviors listed above. Testing approach: Use Fastify's app.inject() for HTTP testing without a real network. Build a test app (no DB needed): create Fastify instance, set validatorCompiler/serializerCompiler, decorate pgPool with a mock object whose query method is vi.fn(). Register healthRoutes. Use app.inject({ method: 'GET', url: '/health' }) to call the route. For error path: mock pgPool.query to throw new Error('DB down'). Import from vitest: describe, it, expect, vi, beforeEach, afterEach. App teardown: afterEach(() => app.close()). Import healthRoutes from '../routes/health.js'.
  </action>
  <verify>
    <automated>cd "C:/Users/Usuario12/OneDrive - HASAS/Obsidian/knowledge/brainny" && npx vitest run src/routes/health.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - vitest run src/routes/health.test.ts exits 0 with 3 tests passing
    - src/plugins/config.ts contains fp(configPlugin, { name: 'config' })
    - src/plugins/db.ts contains fp(dbPlugin, { name: 'db', dependencies: ['config'] })
    - src/plugins/db.ts contains onClose hook calling pool.end()
    - src/routes/health.ts returns 200 on DB ok and 503 on DB error (not always 200)
    - src/index.ts calls both setValidatorCompiler(validatorCompiler) AND setSerializerCompiler(serializerCompiler)
    - src/index.ts calls loadConfig() before Fastify instantiation
    - tsc --noEmit exits 0 across all new files
    - No console.* calls in any src/ file (except the annotated eslint-disable-line in config.ts)
  </acceptance_criteria>
  <done>GET /health integration tests pass; Fastify app compiles and boots; db/config plugins use fp() correctly; both validator and serializer compilers registered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External network → /health | GET request from Docker health checks or ops tools; no auth required |
| Fastify app → PostgreSQL | DATABASE_URL credential leaves process boundary to DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | DATABASE_URL in Pino logs | mitigate | pino redact: ['*.connectionString', '*.DATABASE_URL'] in src/lib/logger.ts |
| T-02-02 | Information Disclosure | /health reveals DB connectivity state | accept | Internal network only (yowanet overlay); liveness probe semantics require this |
| T-02-03 | Tampering | Missing serializerCompiler allows extra fields in responses | mitigate | Both setValidatorCompiler AND setSerializerCompiler called in src/index.ts (FASTIFY-3 mitigation) |
| T-02-04 | Denial of Service | DB unavailable crashes boot | accept | Fast-fail is intentional — pool.query('SELECT 1') at boot prevents silent misconfiguration |
| T-02-SC | Tampering | npm install installs packages | accept | All packages APPROVED per RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
After completing both tasks:
1. npx vitest run src/ — must exit 0, all tests green (config + health)
2. npx tsc --noEmit — must exit 0 across all src/ files
3. npx eslint src --max-warnings 0 — must exit 0
4. Manual smoke test (if DATABASE_URL is available): cp .env.example .env → fill in DATABASE_URL → npm run dev — server must log "Server listening on :3000"
</verification>

<success_criteria>
- src/db/schema.ts defines messages, chats, syncState tables with correct column types (vector(1536), jsonb, boolean)
- HNSW index declared with m: 16, ef_construction: 64 in schema
- All Fastify plugins wrapped with fp() and registered in correct order (configPlugin before dbPlugin)
- Both validator and serializer compilers registered in index.ts
- GET /health integration tests: 200 on DB ok, 503 on DB error — 3 tests passing
- Full vitest run exits 0 (5 config tests + 3 health tests = 8 total)
- tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
